spring:
  datasource:
    url: jdbc:mysql://localhost:3306/piste
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format-sql: true
    database: MYSQL 
    database-platform: org.hibernate.dialect.MySQL8Dialect 